-- [[ DARK HUB V18 - MINIMALIST PRO EDITION ]] --
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")

-- Köhnə interfeysləri mütləq sil
for _, v in pairs(CoreGui:GetChildren()) do
    if v:IsA("ScreenGui") and (v.Name:find("Dark") or v.Name:find("Omur")) then
        v:Destroy()
    end
end

local gui = Instance.new("ScreenGui")
gui.Name = "DarkHubV18"
gui.Parent = CoreGui
gui.ResetOnSpawn = false

local isAuth = false
local menuKey = Enum.KeyCode.K

-- 1. LOGIN PANEL (Minimalist)
local log = Instance.new("Frame")
log.Size = UDim2.new(0, 280, 0, 150)
log.Position = UDim2.new(0.5, -140, 0.5, -75)
log.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
log.BorderSizePixel = 2
log.BorderColor3 = Color3.fromRGB(255, 255, 255)
log.Parent = gui
Instance.new("UICorner", log)

local inp = Instance.new("TextBox")
inp.Size = UDim2.new(0, 200, 0, 30)
inp.Position = UDim2.new(0.5, -100, 0.4, 0)
inp.PlaceholderText = "Kod: dark"
inp.Text = ""
inp.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
inp.TextColor3 = Color3.fromRGB(255, 255, 255)
inp.Parent = log

local btnLog = Instance.new("TextButton")
btnLog.Size = UDim2.new(0, 100, 0, 35)
btnLog.Position = UDim2.new(0.5, -50, 0.7, 0)
btnLog.Text = "LOGIN"
btnLog.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
btnLog.TextColor3 = Color3.fromRGB(0, 0, 0)
btnLog.Font = Enum.Font.GothamBold
btnLog.Parent = log

-- 2. ANA MENU (BLACK & WHITE PRO)
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 450, 0, 350)
Main.Position = UDim2.new(0.5, -225, 0.5, -175)
Main.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Main.BorderSizePixel = 2
Main.BorderColor3 = Color3.fromRGB(255, 255, 255)
Main.Visible = false
Main.Active = true
Main.Draggable = true
Main.Parent = gui
Instance.new("UICorner", Main)

-- SOL PANEL (TABS)
local Tabs = Instance.new("Frame")
Tabs.Size = UDim2.new(0, 110, 1, 0)
Tabs.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Tabs.BorderSizePixel = 0
Tabs.Parent = Main
Instance.new("UICorner", Tabs)

-- SƏHİFƏLƏR
local PlayerPage = Instance.new("ScrollingFrame")
PlayerPage.Size = UDim2.new(1, -130, 1, -20)
PlayerPage.Position = UDim2.new(0, 120, 0, 10)
PlayerPage.BackgroundTransparency = 1
PlayerPage.Visible = true
PlayerPage.Parent = Main

local WorldPage = Instance.new("ScrollingFrame")
WorldPage.Size = UDim2.new(1, -130, 1, -20)
WorldPage.Position = UDim2.new(0, 120, 0, 10)
WorldPage.BackgroundTransparency = 1
WorldPage.Visible = false
WorldPage.Parent = Main

Instance.new("UIListLayout", PlayerPage).Padding = UDim.new(0, 5)
Instance.new("UIListLayout", WorldPage).Padding = UDim.new(0, 5)

-- AYARLAR SİSTEMİ
local conf = { 
    espName = false, espBox = false, espLine = false, 
    spd = false, brk = true, trn = true, stab = true, noclip = false 
}

-- EXIT DÜYMƏSİ (X)
local xBtn = Instance.new("TextButton")
xBtn.Size = UDim2.new(0, 25, 0, 25)
xBtn.Position = UDim2.new(1, -30, 0, 5)
xBtn.Text = "X"
xBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
xBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
xBtn.Parent = Main
xBtn.MouseButton1Click:Connect(function() gui:Destroy() end)

-- FUNKSİYA: DÜYMƏ YARATMA
local function makeToggle(name, parent, key)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(1, -5, 0, 35)
    b.Text = name .. ": OFF"
    b.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.BorderSizePixel = 1
    b.BorderColor3 = Color3.fromRGB(80, 80, 80)
    b.Parent = parent
    Instance.new("UICorner", b)

    b.MouseButton1Click:Connect(function()
        conf[key] = not conf[key]
        b.Text = name .. (conf[key] and ": ON" or ": OFF")
        b.BackgroundColor3 = conf[key] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(25, 25, 25)
        b.TextColor3 = conf[key] and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(255, 255, 255)
    end)
end

-- TAB DÜYMƏLƏRİ
local function makeTab(name, pos, page)
    local t = Instance.new("TextButton")
    t.Size = UDim2.new(1, -10, 0, 45)
    t.Position = pos
    t.Text = name
    t.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    t.TextColor3 = Color3.fromRGB(255, 255, 255)
    t.Parent = Tabs
    t.MouseButton1Click:Connect(function()
        PlayerPage.Visible = false
        WorldPage.Visible = false
        page.Visible = true
    end)
end

makeTab("PLAYER", UDim2.new(0, 5, 0, 10), PlayerPage)
makeTab("WORLD", UDim2.new(0, 5, 0, 60), WorldPage)

-- DÜYMƏLƏRİ ƏLAVƏ ET
makeToggle("ESP NAME", PlayerPage, "espName")
makeToggle("ESP BOX", PlayerPage, "espBox")
makeToggle("ESP LINE", PlayerPage, "espLine")

makeToggle("HIZLI SURUS", WorldPage, "spd")
makeToggle("AKILLI TORMOZ", WorldPage, "brk")
makeToggle("RAHAT DONME", WorldPage, "trn")
makeToggle("STABILLIK", WorldPage, "stab")
makeToggle("CAR NOCLIP", WorldPage, "noclip")

-- LOGIN MƏNTİQİ
btnLog.MouseButton1Click:Connect(function()
    if inp.Text == "dark" then
        isAuth = true
        log.Visible = false
        Main.Visible = true
    else
        inp.PlaceholderText = "WRONG!"
        inp.Text = ""
    end
end)

-- ESP SİSTEMİ (Name, Box, Line) - Polislər Göy, Digərləri Qırmızı
local function createESP(plr)
    local line = Drawing.new("Line")
    local box = Drawing.new("Square")
    local text = Drawing.new("Text")

    RunService.RenderStepped:Connect(function()
        if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") and plr ~= Players.LocalPlayer then
            local hrp = plr.Character.HumanoidRootPart
            local pos, onScreen = workspace.CurrentCamera:WorldToViewportPoint(hrp.Position)
            local color = (plr.Team and plr.Team.Name == "Police") and Color3.new(0, 0, 1) or Color3.new(1, 0, 0)

            if onScreen then
                if conf.espLine then
                    line.Visible = true
                    line.From = Vector2.new(workspace.CurrentCamera.ViewportSize.X / 2, workspace.CurrentCamera.ViewportSize.Y)
                    line.To = Vector2.new(pos.X, pos.Y)
                    line.Color = color
                else line.Visible = false end

                if conf.espBox then
                    box.Visible = true
                    box.Size = Vector2.new(2000 / pos.Z, 2500 / pos.Z)
                    box.Position = Vector2.new(pos.X - box.Size.X / 2, pos.Y - box.Size.Y / 2)
                    box.Color = color
                    box.Thickness = 1
                else box.Visible = false end

                if conf.espName then
                    text.Visible = true
                    text.Text = plr.Name
                    text.Position = Vector2.new(pos.X, pos.Y - (2500 / pos.Z) / 2 - 15)
                    text.Color = color
                    text.Center = true
                    text.Outline = true
                else text.Visible = false end
            else
                line.Visible = false box.Visible = false text.Visible = false
            end
        else
            line.Visible = false box.Visible = false text.Visible = false
        end
    end)
end

for _, p in pairs(Players:GetPlayers()) do createESP(p) end
Players.PlayerAdded:Connect(createESP)

-- FİZİKA (Sənin istədiyin sabit 0.08 dönmə ilə)
RunService.Stepped:Connect(function()
    if not isAuth then return end
    local char = Players.LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        local seat = char.Humanoid.SeatPart
        if seat and seat:IsA("VehicleSeat") then
            if conf.spd and seat.Throttle == 1 then
                seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity + (seat.CFrame.LookVector * 1.8)
            end
            if conf.brk and seat.Throttle == -1 then
                local v = seat.CFrame:VectorToObjectSpace(seat.AssemblyLinearVelocity)
                if v.Z < -2 then seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity * 0.96 end
            end
            if conf.trn and seat.Steer ~= 0 then
                seat.AssemblyAngularVelocity = seat.AssemblyAngularVelocity + Vector3.new(0, -seat.Steer * 0.08, 0)
            end
            if conf.stab then
                seat.AssemblyLinearVelocity = seat.AssemblyLinearVelocity + Vector3.new(0, -1.5, 0)
            end
            if conf.noclip then
                for _, part in pairs(seat.Parent:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            else
                for _, part in pairs(seat.Parent:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = true end
                end
            end
        end
    end
end)

-- GİZLƏ/AÇ
UserInputService.InputBegan:Connect(function(i, g)
    if isAuth and not g and i.KeyCode == menuKey then Main.Visible = not Main.Visible end
end)
